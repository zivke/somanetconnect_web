'use strict';

describe('myApp.deviceList module', function() {

  beforeEach(module('myApp.deviceList'));

  describe('deviceList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var deviceListCtrl = $controller('deviceListCtrl');
      expect(deviceListCtrl).toBeDefined();
    }));

  });
});