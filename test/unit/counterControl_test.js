'use strict';

describe('myApp.counterControl module', function() {

  beforeEach(module('myApp.counterControl'));

  describe('counterControl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var counterControlCtrl = $controller('counterControlCtrl');
      expect(counterControlCtrl).toBeDefined();
    }));

  });
});